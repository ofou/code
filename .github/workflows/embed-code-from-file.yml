name: Embed Code From File

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Markdown Embed Code From File
        uses: tokusumi/markdown-embed-code@v1.0.0
